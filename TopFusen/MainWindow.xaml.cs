using System.Windows;

namespace TopFusen;

/// <summary>
/// MainWindow — Phase 0 では使用しない（トレイ常駐のため）
/// Phase 1 以降で不要になる予定
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
